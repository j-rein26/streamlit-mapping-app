# Mapping App
### This app allows the user to view a map that has markers which are read from a google sheets document. The document contains addresses that have been geocoded and then ordered according to proximity to each other.
### This allows those visiting the addresses to see which locations are near each other and visit accordingly. 
